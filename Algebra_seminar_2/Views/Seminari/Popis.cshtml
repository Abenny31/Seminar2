﻿@model IEnumerable<Algebra_seminar_2.Models.Seminar>

@{
    ViewBag.Title = "Popis";
}

<h2>Popis</h2>

<div>
    <h4>Seminar</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>

<table class="table" border="1" style="text-align:center;" )
    <tr >
        <th style="text-align:center">
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th style="text-align:center">
            @Html.DisplayNameFor(model => model.Opis)
        </th>
        <th style="text-align:center">
            @Html.DisplayNameFor(model => model.Datum)
        </th>
    </tr>

      @foreach ( var item in Model )
    {
    <tr>
        @Html.HiddenFor(modelItem => item.IdSeminar)
        <td>@Html.DisplayFor(modelItem  => item.Naziv)</td>
        <td>@Html.DisplayFor(modelItem => item.Opis)</td>
        <td>@Html.DisplayFor(modelItem => item.Datum)</td>
        <td>@Html.ActionLink("Predbiljezba", "Predbiljezba", new { id = item.IdSeminar})</td>
        @*<td>@Html.ActionLink("Izbriši","Izbrisi", new { id = item.ID })</td>*@
    </tr>
    }
    </table>

<p>
    @*@Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
        @Html.ActionLink("Back to List", "Index")*@
</p>
